<p><%= link_to 'New Artwork', new_artwork_path %></p>
<% @artworks.each do |artwork| %>
  <div class="photo">
    <a href="#">
  	  <span></span>
  	  <%= image_tag artwork.photo.url %>
    </a>
  </div>
<% end %>
